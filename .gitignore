# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production build
/build

# local file-storage fallback (used when GCS_BUCKET is unset)
/.uploads/

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── secrets — never commit these ───
.env
.env.*
!.env.example
*.creds.json
.creds.json
service-account*.json
*-key.json
application_default_credentials.json
gha-creds-*.json

# ─── Local editor / tooling artifacts ───
.idea/
.vscode/
.cursor/
*.swp

# typescript / build info
*.tsbuildinfo

# prisma local db
/prisma/*.db

# test output
/coverage

# generated / local-only sample artifacts (not part of the submission)
samples/sample-rfp-internet.pdf
samples/last-review.json
